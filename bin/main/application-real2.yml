server:
  port: 8083

spring:
  profiles:
    include:
      - oauth
      - real-db
  session:
    store-type: jdbc

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        storage_engine: innodb
